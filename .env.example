# Copia a .env.
# backend: "fake" (dev/tests) o "pixelrag" (real, requiere modelo Qwen3-VL + poppler).
BACKEND=fake
DATA_DIR=./data
RENDER_DPI=200
DEFAULT_N_DOCS=5

# PixelRAG real
PIXELRAG_SERVE_URL=http://localhost:30001
PIXELRAG_MODEL=Qwen3-VL-Embedding-2B
PIXELRAG_GPU_IDS=
